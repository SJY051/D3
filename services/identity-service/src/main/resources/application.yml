server:
  port: 8081

spring:
  application:
    name: identity-service
  config:
    import: optional:configserver:${CONFIG_SERVER_URL:http://localhost:8888}
  datasource:
    url: ${IDENTITY_DB_URL:jdbc:postgresql://localhost:5432/d3_identity}
    username: ${IDENTITY_DB_USER:d3_identity}
    password: ${IDENTITY_DB_PASSWORD:local-only}
  jpa:
    hibernate:
      ddl-auto: validate

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URL:http://localhost:8761/eureka/}

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
