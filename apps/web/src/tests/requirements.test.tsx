import { describe, expect, it } from "vitest";

describe.skip("D3-UX-001 and D3-UX-002 acceptance", () => {
  it("matches approved wireframes at desktop and mobile widths", () => {
    expect.fail("Enable after the team approves WF-01 through WF-08");
  });

  it("supports the deterministic ranked battle demonstration path", () => {
    expect.fail("Enable after the ranked vertical slice exists");
  });
});
